use anyhow::Result;

// create short cut
pub fn create_shortcuts(
    app_name: &str,
    target: &str,
    args: Option<&str>,
    icon: Option<&str>,
) -> Result<()> {
    Ok(())
}

fn known_folder() -> Result<()> {
    Ok(())
}

fn save_link() -> Result<()> {
    Ok(())
}

pub fn remove_shortcuts(app_name: &str) -> Result<()> {
    Ok(())
}